APP_ENV=local

API_V1_PREFIX=/api/v1
SECRET_KEY=sm-lg@Aklf
ACCESS_TOKEN_EXPIRE_MINUTES=60
REFRESH_TOKEN_EXPIRE_DAYS=7

BACKEND_CORS_ORIGINS=["http://localhost:5173","http://127.0.0.1:5173"]

DATABASE_URL=postgresql+psycopg2://hms_user:hms_password@localhost:5434/hms_db
REDIS_URL=redis://localhost:6379/0

# web push
VAPID_PUBLIC_KEY=VAPID_PUBLIC_KEY
VAPID_PRIVATE_KEY=VAPID_PRIVATE_KEY
VAPID_SUB=mailto:your_email@example.com

RESEND_API_KEY=re_your_key

EMAIL_BACKEND=resend
#Resend sends email only to registered email when domain is not verified.

EMAIL_FROM_NAME=HMS
EMAIL_FROM=noreply@example.com
EMAIL_SANDBOX_MODE=true
EMAIL_TEST_RECIPIENT=your_email@example.com

EMAIL_SMTP_HOST=mail.example.com
EMAIL_SMTP_PORT=465
EMAIL_SMTP_USERNAME=user@example.com
EMAIL_SMTP_PASSWORD=your-password
EMAIL_USE_TLS=true

# Email to patients
SEND_EMAIL_TO_PATIENTS=True

# SMS to patients (requires SMS_ENABLED=True and provider config)
SEND_SMS_TO_PATIENTS=True
SMS_ENABLED=True
SMS_PROVIDER=twilio
SMS_PROVIDER_ACCOUNT_SID=your_account_sid
SMS_PROVIDER_AUTH_TOKEN=your_auth_token
SMS_FROM_NUMBER=+1234567890

WHATSAPP_ENABLED=false
WHATSAPP_PROVIDER=none

HMS_DEV_ALLOW_TENANT_SCHEMA_RESET = true
SCHEMA_NAME_MAX_ATTEMPTS = 5

DEMO_MODE=true  # Enable demo refresh
VITE_DEMO_MODE=true  # Show demo account
DEMO_AUTO_REFRESH_ON_LOGIN=true
DEMO_FRESHEN_DAYS=7
DEMO_REFRESH_TTL_HOURS=24

# Super Admin (platform-level, tenant_id NULL)
SUPER_ADMIN_EMAIL=sadmin@hmadmin.hms
SUPER_ADMIN_PASSWORD=fgXrt345@&1

# Optional
SUPER_ADMIN_FIRST_NAME=Super
SUPER_ADMIN_LAST_NAME=Admin